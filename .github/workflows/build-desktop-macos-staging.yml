# Builds the staging macOS ZIP on dev (artifact only — no GitHub Release).
# Publish to Releases: tag v*.*.*-staging → release-desktop-staging.yml
name: Build desktop (macOS, staging)

on:
  workflow_dispatch:
  push:
    branches: [dev, develop]
    paths:
      - 'apps/desktop/**'
      - 'packages/shared/**'
      - 'pnpm-lock.yaml'
      - '.github/workflows/build-desktop-macos-staging.yml'
  pull_request:
    branches: [dev, develop]
    paths:
      - 'apps/desktop/**'
      - 'packages/shared/**'
      - 'pnpm-lock.yaml'
      - '.github/workflows/build-desktop-macos-staging.yml'

concurrency:
  group: desktop-macos-staging-${{ github.ref }}
  cancel-in-progress: true

env:
  GH_OWNER: ${{ vars.GH_OWNER || github.repository_owner }}
  GH_REPO: ${{ vars.GH_REPO || 'connect' }}
  RX_CONNECT_CHANNEL: staging
  NEXT_PUBLIC_API_BASE_URL: ${{ vars.STAGING_API_BASE_URL || 'https://api.staging.onerx.com/api' }}
  NEXT_PUBLIC_RX_CONNECT_INGEST_SECRET: ${{ secrets.RX_CONNECT_INGEST_SECRET }}
  NEXT_PUBLIC_RX_CONNECT_DEV_SKIP_AUTH: 'true'
  RX_CONNECT_DEV_SKIP_AUTH: 'true'

jobs:
  build-macos-staging:
    runs-on: macos-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile
        env:
          HUSKY: '0'

      - name: Rebuild native modules for Electron
        run: pnpm exec electron-rebuild -f -w keytar
        working-directory: apps/desktop

      - name: Build shared package
        run: pnpm --filter @rx-connect/shared run build

      - name: Build renderer (staging)
        run: pnpm --filter @rx-connect/desktop run build:renderer:staging

      - name: Vendor Hugo (macOS arm64)
        run: node apps/desktop/scripts/download-hugo.cjs --platform=current

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22.x'

      - name: Go mod tidy (print service)
        run: cd services/print-service && go mod tidy

      - name: Build macOS print service
        run: node apps/desktop/scripts/build-print-service.cjs

      - name: Build staging macOS ZIP
        run: pnpm --filter @rx-connect/desktop run dist:mac:staging
        env:
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}

      - name: Validate staging updater metadata
        run: |
          if [ ! -f apps/desktop/dist/staging-mac.yml ]; then
            echo "staging-mac.yml was not generated."
            exit 1
          fi

      - name: List build output
        run: ls -laR apps/desktop/dist

      - name: Upload staging macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: rx-connect-macos-staging-${{ github.run_number }}
          path: |
            apps/desktop/dist/*.zip
            apps/desktop/dist/*.zip.blockmap
            apps/desktop/dist/staging-mac.yml
          if-no-files-found: error
          retention-days: 30
