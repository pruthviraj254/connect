# Windows installer build assets

- `installer.nsh` — NSIS hooks for virtual-printer install/uninstall during setup.
- Add `icon.ico` (256×256 recommended) and optional `installerHeader.bmp` / `installerSidebar.bmp`, then uncomment the matching keys in `electron-builder.yml`.
